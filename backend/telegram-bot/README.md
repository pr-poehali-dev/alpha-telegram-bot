# Telegram Бот для Колл-Центра Альфа-Банк

## Описание

Полноценный Telegram бот для управления заявками колл-центра с базой данных PostgreSQL.

## Возможности

- ✅ Меню команд для администраторов
- ✅ Список активных заявок с приоритетами
- ✅ Блокировка карт и приложений
- ✅ Перевыпуск карт
- ✅ Журнал аудита всех операций
- ✅ Статистика работы
- ✅ База данных PostgreSQL

## Настройка

### 1. Добавьте токен бота

В настройках проекта добавьте секрет `TELEGRAM_BOT_TOKEN`:
```
8555997475:AAHegFCmXVzlpTG3x9mAUMjMyrpARoNiGjk
```

### 2. Настройте webhook

Выполните команду для установки webhook:

```bash
python backend/telegram-bot/setup_webhook.py
```

Или вручную через curl:

```bash
curl -X POST "https://api.telegram.org/bot8555997475:AAHegFCmXVzlpTG3x9mAUMjMyrpARoNiGjk/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{
    "url": "https://functions.poehali.dev/475a3d5b-8b0b-413b-b4dd-71318038759e",
    "allowed_updates": ["message", "callback_query"]
  }'
```

### 3. Установите команды бота

```bash
curl -X POST "https://api.telegram.org/bot8555997475:AAHegFCmXVzlpTG3x9mAUMjMyrpARoNiGjk/setMyCommands" \
  -H "Content-Type: application/json" \
  -d '{
    "commands": [
      {"command": "start", "description": "Начать работу с ботом"},
      {"command": "menu", "description": "Главное меню"},
      {"command": "requests", "description": "Список активных заявок"},
      {"command": "stats", "description": "Статистика работы"},
      {"command": "help", "description": "Справка по командам"}
    ]
  }'
```

## Команды бота

- `/start` - Начало работы, регистрация администратора
- `/menu` - Главное меню с кнопками операций
- `/requests` - Список активных заявок
- `/stats` - Статистика по заявкам
- `/help` - Справка по командам

## Интерактивное меню

Бот поддерживает кнопки для быстрых действий:
- 🛡️ Блокировка карты
- 📱 Блокировка приложения
- 💳 Перевыпуск карты
- 📋 Список заявок

## База данных

Структура таблиц:

- `clients` - Клиенты банка
- `requests` - Заявки от клиентов
- `audit_logs` - Журнал всех операций
- `admins` - Администраторы бота

## Webhook URL

```
https://functions.poehali.dev/475a3d5b-8b0b-413b-b4dd-71318038759e
```

## Тестирование

После настройки откройте бота в Telegram и отправьте команду `/start`

Бот должен ответить приветственным сообщением и зарегистрировать вас как администратора.

## Добавление тестовых данных

Для демонстрации работы бота добавьте тестовые заявки через SQL:

```sql
-- Добавить тестового клиента
INSERT INTO clients (phone, full_name, email, card_number)
VALUES ('+7 999 123 45 67', 'Иванов Иван Иванович', 'ivanov@example.com', '**** 1234');

-- Добавить тестовую заявку
INSERT INTO requests (client_id, request_type, priority, status, description, telegram_chat_id)
VALUES (1, 'Блокировка карты', 'high', 'pending', 'Утеря карты', NULL);
```

## Логи и отладка

Проверьте логи функции в панели poehali.dev для отладки работы бота.
